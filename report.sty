\ProvidesPackage{report}

% Margins
\usepackage[top=2.5cm,bottom=2.5cm,left=2cm,right=2cm]{geometry}

% Languages
\usepackage[english]{babel}

% Fonts
\usepackage[T1]{fontenc}
\usepackage{libertine} % Linux Libertine font

% Spacing
\usepackage{setspace}
\onehalfspacing % Line spacing
\setlength{\parskip}{12pt} % Paragraph spacing
\usepackage{enumitem} % For list (itemize, enumerate)

% Maths
\usepackage{amsmath,amsfonts,amssymb}
\usepackage{bbm}

% Figures
\usepackage{graphicx}
\usepackage{subcaption}
\graphicspath{{./img/}} % Images path

% Tables
\usepackage{multirow}
\usepackage{booktabs}

% Colours
\usepackage[dvipsnames]{xcolor}

% Hyperlinks
\usepackage[hyphens]{url}
\urlstyle{same}
\usepackage[linktocpage=true, colorlinks=true, citecolor=teal, linkcolor=BrickRed, urlcolor=blue, breaklinks=true]{hyperref}

% References
\usepackage[style=ieee, url=false, dashed=false]{biblatex} % tldr, better than BibTeX
\addbibresource{references.bib} % Bibliography file
\usepackage{csquotes}

% Appendix
\usepackage{appendix}

% Page # of ##
\usepackage{lastpage}
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhead{}
\renewcommand{\headrulewidth}{0pt}
\cfoot{\thepage\ of \pageref{LastPage}}

% Misc
\usepackage{listings} % Copy Code in a latex document
\usepackage{microtype} % Improve typesetting (+ fewer bad boxes problems)
\usepackage{titling} % Provides macros \thetitle, \theauthor and \thedate to be used anywhere in the document
\usepackage{comment} % Comment multiples lines
% \usepackage{pdfpages} % Insert pdf document
% \usepackage{wrapfig} % % Wrapping text around figures

%%%%% NEW COMMANDS

% Horizontal lines (change thickness)
\newcommand{\HRule}{\rule{\linewidth}{0.5mm}} 

% Title
\makeatletter
\renewcommand{\maketitle}{
\begin{center}
\textsc{\Large \bfseries \@title }\par
\@author -- \@date \par
\end{center}
\HRule}
\makeatother